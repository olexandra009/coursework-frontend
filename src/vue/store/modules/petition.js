import Vue from "vue";

const state = () => ({all: [], currentPage: -1, totalPages: 0});
const getters={};
const actions={};
const mutations={};

export default {
    namespaced: 'petition',
    state,
    getters,
    actions: actions,
    mutations: mutations,
};