import Vue from "vue";

const state = () => ({all: [], currentPage: -1, totalPages: 0});
const getters={};
const action={};
const mutation={};

export default {
    namespaced: 'events',
    state,
    getters,
    actions: actions,
    mutations: mutations,
};